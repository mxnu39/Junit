# Aufgabe / 07.12.2025
<h2>Aufgabenstellung: Schüler sortieren</h2>

<strong>Ziel:</strong>
Du sollst die Schueler-Objekte in deiner Liste Schuelerliste nach unterschiedlichen Kriterien sortieren. Dazu sollst du Comparable<Schueler> oder einen Comparator verwenden.

<strong>Schritte:</strong>
<strong>a) </strong>Füge der Klasse Schueler die Implementierung von Comparable<Schueler> hinzu.
<strong>b) </strong>Implementiere die compareTo()-Methode, sodass Schüler standardmäßig nach Name alphabetisch sortiert werden.
<strong>c) </strong>Erstelle in main() eine Methode, die die Liste Schuelerliste sortiert und anschließend ausgibt.

<strong>Erweiterung (optional, schwerer):</strong>
Sortiere zusätzlich die Schüler nach:

<strong>ID-Nummer (aufsteigend)</strong>

<strong>AG-Zuteilung (AG 1 → AG 5)</strong>

<strong>Alter (falls du age bei den Objekten gesetzt hast)</strong>

<strong>Ausgabe:</strong>
Gib nach der Sortierung die Schülerliste in der Konsole aus, z. B.:


# Aufgabe A – Interface

Erstelle ein <strong>Interface</strong> "Elektronisch" mit folgenden Methoden:

einschalten()

ausschalten()

Dann:

Erstelle eine Klasse "<strong>Computer</strong>", die das Interface implementiert

Die Methoden sollen <strong>sinnvolle Ausgaben</strong> machen

Teste alles in einer <strong>Main-Klasse</strong>